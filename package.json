{
  "name": "ptc_test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client:i": "cd client && npm i",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build"
  },
  "dependencies": {
   
  },
  "devDependencies": {
   
  }
}
